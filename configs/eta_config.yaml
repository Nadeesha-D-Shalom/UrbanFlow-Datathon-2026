# Starter configuration; verify schema and split dates before training.
task: eta
target: trip_duration_minutes
random_seed: 42
output_dir: models/eta
split_strategy: chronological
train_end: null
validation_end: null
metrics: [mae, rmse, r2]
